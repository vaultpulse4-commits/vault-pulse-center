# DomaiNesia PostgreSQL Configuration (TEST)
# Ganti dengan credentials dari cPanel PostgreSQL Database Wizard

# Format: postgresql://username:password@host:port/database
# Password dengan special characters harus di-encode
# e]]14xosNvr{5#C( → e%5D%5D14xosNvr%7B5%23C%28
DATABASE_URL="postgresql://rappwebi_admin:e%5D%5D14xosNvr%7B5%23C%28@localhost:5432/rappwebi_vault_pulse"

PORT=3001
NODE_ENV=production
FRONTEND_URL=https://rapp.web.id
JWT_SECRET=your-random-secret-key-minimum-32-chars-change-this-in-production
CORS_ORIGIN=https://rapp.web.id

# Notes:
# 1. Login ke cPanel DomaiNesia
# 2. Go to "PostgreSQL Database Wizard"
# 3. Create database: rappwebi_vaultdb
# 4. Create user: rappwebi_vaultuser (with strong password)
# 5. Grant ALL permissions
# 6. Copy connection details ke file ini
# 7. Test dengan: npm run test:connection

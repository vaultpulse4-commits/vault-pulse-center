# Production Environment Configuration
# Copy this file to /home/rappwebi/vault-pulse-center/server/.env

# Database Configuration
# Format: postgresql://username:password@host:port/database
DATABASE_URL="postgresql://rappwebi_user:YOUR_PASSWORD_HERE@localhost:5432/rappwebi_vault_pulse_db"

# Server Configuration
PORT=3001
NODE_ENV=production

# Frontend URL
FRONTEND_URL=https://rapp.web.id

# JWT Secret (CHANGE THIS!)
# Generate random: openssl rand -base64 32
JWT_SECRET=vault-pulse-super-secret-jwt-key-change-this-to-random-32-chars-minimum

# CORS Origin
CORS_ORIGIN=https://rapp.web.id

# Optional: Email Configuration (jika mau enable email notifications)
# SMTP_HOST=smtp.gmail.com
# SMTP_PORT=587
# SMTP_USER=your-email@gmail.com
# SMTP_PASS=your-app-password
# EMAIL_FROM=noreply@rapp.web.id
